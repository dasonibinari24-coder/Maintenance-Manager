document.querySelector('#appointment-form').addEventListener('keydown',e=>{if(e.key!=='Enter'||e.target.tagName==='TEXTAREA')return;e.preventDefault();const active=document.querySelector('.wizard-step.active');const fields=[...active.querySelectorAll('input,select')].filter(x=>!x.disabled);const i=fields.indexOf(e.target);if(i>=0&&i<fields.length-1){fields[i+1].focus();return}if(!document.querySelector('#next-step').hidden)document.querySelector('#next-step').click();});
